services:
  llmania:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      SESSION_SECRET: ${SESSION_SECRET:-change-this-in-production}
      PORT: 3000
    volumes:
      - ./data:/app/server/data
    restart: unless-stopped
